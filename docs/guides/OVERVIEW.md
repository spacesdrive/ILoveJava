# Guides

Longer how-tos that don't fit inside a single `docs/` section. Empty for now - the first guide will likely be "adding a new feature module" once the first real feature (lessons) exists and there's a concrete example to document from, rather than a hypothetical one.
