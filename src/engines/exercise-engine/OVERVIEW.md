# exercise-engine

Defines `ExerciseContent` (prompt, starter code, hidden/visible test cases, hints) and the shape of a run result. Depends on the playground engine to actually execute Java code - see [../playground-engine](../playground-engine) for the open question of how Java runs in the browser.

Not yet implemented: the exercise runner UI, hint disclosure logic, attempt history persistence.
